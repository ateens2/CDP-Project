# 🪟 Windows 사용법 가이드

## 🚀 빠른 시작

### 1️⃣ 서버 시작하기
프로젝트 폴더에서 다음 중 하나를 **더블클릭**하세요:

- **`start_server.bat`** - 완전한 시작 (의존성 설치 + 빌드 + 서버 시작)
- **`quick_start.bat`** - 빠른 시작 (서버만 시작)

### 2️⃣ 자동 실행 과정
1. 🔍 Node.js 확인
2. 📦 필요시 의존성 설치
3. 🔨 필요시 프론트엔드 빌드  
4. 🚀 서버 시작
5. 🌐 **3초 후 자동으로 브라우저에서 홈페이지 열림** (`http://localhost:3000`)

### 3️⃣ 서버 종료하기
- **`stop_server.bat`** 더블클릭
- 또는 서버 실행 창에서 `Ctrl + C`

## 📁 배치 파일 설명

### `start_server.bat` (권장)
- ✅ 완전한 자동화
- ✅ Node.js 설치 확인
- ✅ 의존성 자동 설치
- ✅ 프론트엔드 자동 빌드
- ✅ 에러 처리 포함
- ✅ 브라우저 자동 열기

### `quick_start.bat` (빠른 실행)
- ⚡ 빠른 시작
- ⚠️ 사전에 빌드 완료된 상태여야 함
- ✅ 브라우저 자동 열기

### `stop_server.bat`
- 🛑 서버 완전 종료
- 🧹 모든 Node.js 프로세스 정리

## 🔧 사전 요구사항

### 필수 설치
1. **Node.js** (v16 이상)
   - 다운로드: https://nodejs.org
   - 설치 확인: 명령 프롬프트에서 `node --version`

### 권장 설정
- Windows 10/11
- 관리자 권한 (포트 사용을 위해)

## 🌐 접속 정보
- **웹사이트**: http://localhost:3000
- **API**: http://localhost:3000/api

## ❗ 문제 해결

### 🔧 디버깅 도구

#### CMD창이 바로 닫히는 경우
1. **`debug_server.bat`** 더블클릭 - 상세한 에러 정보 확인
2. **`test_node.bat`** 더블클릭 - 기본 환경 확인

#### 단계별 디버깅
1. **`test_node.bat`** - Node.js 설치 및 폴더 구조 확인
2. **`debug_server.bat`** - 상세한 문제 분석
3. **`start_server.bat`** - 정상 실행

### 🚨 일반적인 문제들

#### 서버가 시작되지 않는 경우
1. **Node.js 미설치**
   - [Node.js 공식 사이트](https://nodejs.org)에서 LTS 버전 설치
   - 설치 후 컴퓨터 재시작

2. **잘못된 위치에서 실행**
   - 프로젝트 루트 폴더(backend, frontend가 보이는 곳)에서 실행
   - `test_node.bat`으로 폴더 구조 확인

3. **의존성 설치 실패**
   - 인터넷 연결 확인
   - 관리자 권한으로 실행
   - 바이러스 백신 일시 중지

#### 브라우저가 자동으로 열리지 않는 경우
- 수동으로 `http://localhost:3000` 접속

#### 포트 3000이 사용 중인 경우
- `stop_server.bat` 실행 후 다시 시도
- 작업 관리자에서 node.exe 프로세스 종료

#### "사이트에 연결할 수 없음" 오류
1. 서버가 실제로 시작되었는지 확인
2. `debug_server.bat`으로 에러 메시지 확인
3. 방화벽에서 Node.js 허용
4. localhost 대신 `127.0.0.1:3000` 시도

## 🎯 개발 모드

개발 중에 코드를 수정하는 경우:

### 프론트엔드 수정 시
```cmd
cd frontend
npm run build
```
빌드 후 서버 재시작 없이 변경사항 반영됨

### 백엔드 수정 시
서버 재시작 필요 (`stop_server.bat` → `quick_start.bat`)

## 📞 지원

문제가 발생하면:
1. `start_server.bat`로 완전 재설치 시도
2. Node.js 최신 버전 설치
3. 관리자 권한으로 실행 